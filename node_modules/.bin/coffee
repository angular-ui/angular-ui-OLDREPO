#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../coffee-script/bin/coffee" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../coffee-script/bin/coffee" "$@"
  ret=$?
fi
exit $ret
