#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../coffee-script/bin/cake" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../coffee-script/bin/cake" "$@"
  ret=$?
fi
exit $ret
